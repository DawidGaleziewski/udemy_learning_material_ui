We can use component prop to use all styles on other component like react routers Link

```javascript
                  <Tab component={Link}
                  to="/" className={classes.tab} label="Home" />

```